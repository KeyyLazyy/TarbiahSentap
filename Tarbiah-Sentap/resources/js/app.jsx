import React from 'react';
import ReactDOM from 'react-dom/client';
import BookstoreApp from './BookstoreApp.jsx';
import '../css/app.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BookstoreApp />
  </React.StrictMode>,
);
